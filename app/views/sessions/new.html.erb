<h1>Login</h1>

<%= form_with(scope: :session, local: true, url: sessions_path) do |f| %>
  <%= f.label :email %>
  <%= f.email_field :email %>
  <%= f.label :password %>
  <%= f.password_field :password %>
  <%= f.submit 'log in' %>
<% end %>